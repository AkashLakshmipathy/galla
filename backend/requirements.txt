# The litellm extra is not optional for us: core/model.py reaches Vertex through
# strands.models.litellm, and without it the import fails at runtime, model()
# returns None and every agent silently runs its deterministic fallback — a
# container that builds clean and does no AI at all.
strands-agents[litellm]>=1.54.0
strands-agents-tools>=0.8.8
google-cloud-firestore>=2.16.0
google-cloud-storage>=2.16.0
google-cloud-pubsub>=2.21.0
google-cloud-logging>=3.10.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
reportlab>=4.1.0
rapidfuzz>=3.6.0
httpx>=0.27.0
pytest>=8.0.0
pillow>=10.0.0
